# Optional: a private X Layer Testnet RPC URL (your own node / provider).
# Without this the app falls back to the public endpoint (https://testrpc.xlayer.tech),
# which is rate-limited and caps getLogs at 100 blocks.
NEXT_PUBLIC_RPC_URL=https://testrpc.xlayer.tech
